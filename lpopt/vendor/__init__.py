"""Vendored third-party / upstream snapshots pinned into lpopt.

Currently: ``masterrl`` — a byte-pinned SHA-256 snapshot subset of the 3_GA
``master_rl`` harness (see ``masterrl/VENDOR_MANIFEST.json``).
"""
